// 게임 화면 Rendering 을 위한 Window 구조체
public struct RenderWindow
{
    public int Width;
    public int Height;
    public RenderWindow(int width, int height)
    {
        Width = width;
        Height = height;
    }
}